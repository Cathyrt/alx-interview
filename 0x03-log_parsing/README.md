## 0x03-log parsing

- `0x03-log_parsing` is a log parsing script that reads input from the standard input (stdin) line by line.
- It computes metrics from the input log and prints statistics at regular intervals or upon receiving a keyboard interruption (CTRL + C). 
- The script collects information about the total file size and counts theoccurrences of each HTTP status code in the input log.
